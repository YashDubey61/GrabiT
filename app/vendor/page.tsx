import { ScreenStub } from "@/components/shared/ScreenStub";

export default function VendorActiveOrdersPage() {
  return (
    <ScreenStub
      title="Active Orders"
      stitchSource="grabit_vendor_active_orders_premium_black/code.html (+ _mobile variant, responsive by breakpoint, not a separate route)"
      role="Vendor"
    />
  );
}
