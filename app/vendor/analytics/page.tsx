import { ScreenStub } from "@/components/shared/ScreenStub";

export default function VendorAnalyticsPayoutsPage() {
  return (
    <ScreenStub
      title="Analytics & Payouts"
      stitchSource="grabit_vendor_analytics_payouts_premium_black/code.html (+ _mobile variant). Note: a non-Premium-Black variant also exists (grabit_vendor_analytics_payouts/) — superseded, see Day 1 report §10."
      role="Vendor"
    />
  );
}
