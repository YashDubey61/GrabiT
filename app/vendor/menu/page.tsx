import { ScreenStub } from "@/components/shared/ScreenStub";

export default function VendorMenuManagementPage() {
  return (
    <ScreenStub
      title="Menu Management"
      stitchSource="grabit_vendor_menu_management_premium_black/code.html (+ _mobile variant, responsive by breakpoint, not a separate route)"
      role="Vendor"
    />
  );
}
